[project]
name = "openmdm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mockdatagen>=1.1.0",
    "networkx>=3.5",
    "recordlinkage>=0.16",
]
